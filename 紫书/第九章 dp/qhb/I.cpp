#include <iostream>
#include <cstdio>
#include <cstring>
#include <string>
using namespace std;
int l, n, a[55];
int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	while (cin >> l && l) {
		cin >> n;
		for (int i = 1; i <= n; ++i) cin >> a[i];
		
	}
}
