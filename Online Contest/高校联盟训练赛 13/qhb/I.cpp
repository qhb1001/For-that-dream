#include <cmath>
#include <iostream>
#include <cstring>
using namespace std;
const double PI = 3.14159265;
int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);

	double L = 2;
	cout << (L*L) / (4*PI) << endl;
	cout << sqrt(3) * L * L / 36 << endl;
}
